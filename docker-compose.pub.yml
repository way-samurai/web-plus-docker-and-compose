version: '3'

services:
  backend:
    container_name: 'kupipodariday-backend'
    image: waysamurai/kupipodariday-backend:1.0.0
    env_file: .env
    restart: always
    ports:
      - '4000:3000'
    depends_on:
      - postgres
    networks: 
      - backend

  frontend:
    container_name: 'kupipodariday-frontend'
    image:  waysamurai/kupipodariday-frontend:1.0.0
    restart: always
    ports:
      - '3001:3000'
    networks:
      - frontend

  postgres:
    container_name: 'kupipodariday-database'
    image: postgres:12.12-alpine
    restart: always
    env_file: .env
    volumes: 
      - ~/pg_data:/var/lib/postgresql/data
    
    networks:
      - backend

  adminer:
    image: adminer
    restart: always
    ports: 
      - 8080:8080
    depends_on:
      - postgres  
    networks: 
      - backend

volumes: 
  postgres_data: 

networks:
  backend:
  frontend: